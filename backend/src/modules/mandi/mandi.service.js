
export const getMandiPrices = async (crop, state) => {
    // Temporary response until real API is connected
    return {
        crop,
        state,
        message: "Mandi price service is ready"
    };
};